model:
  name: "gbr"
  n_estimators: 100
  max_depth: 5
  learning_rate: 0.1

data:
  input_data_path: "data/experiments/"

training:
  # GBR doesn't use these but included for consistency
  batch_size: 64

logging:
  log_interval: 10
  save_model: true
